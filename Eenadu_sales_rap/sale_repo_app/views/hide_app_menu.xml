<odoo>
    <data noupdate="1">

        <!-- Override 'Discuss' menu to make it visible only to admin (optional) -->
        <record id="base.menu_management" model="ir.ui.menu">
            <!-- Only show to admin or remove all groups to hide from everyone -->
            <field name="groups_id" eval="[(6, 0, [ref('base.group_system')])]"/>
        </record>

    </data>
</odoo>
